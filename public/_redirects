# Old HubSpot URLs. Order matters — Netlify stops at the first match.
# /contact points at the waitlist form rather than the bare homepage, so the
# old link lands somewhere with an action on it.

/contact             /#notify   301
/blog/*              /          301
/starter-package     /          301
/premium-package     /          301
