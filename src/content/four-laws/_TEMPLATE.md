---
# Copy this file, rename it to the page's live path, delete these comments.
#
# The filename becomes the URL. `conways-law-revops-team-structure.md` is served
# at /conways-law-revops-team-structure — which is exactly where it lives on
# fourlaws.netlify.app today. Match the old path exactly; outreach links are
# already pointing at these.
#
# Leading underscore keeps this template out of the build.
title: Page title as it appears in the <h1>
description: One or two sentences. Used for the meta description and the card on the home page, so write it for a stranger scanning search results.
group: explore # reference | explore | meta
order: 100 # sparse numbering — 10, 20, 30 — so pages can be slotted in later
draft: true # flip to false to publish
---

Body copy in markdown.

Internal links use site-relative paths — [the glossary](/glossary), not the full
fourlaws.netlify.app URL. Absolute links to the old domain will keep working
after cutover, but they send readers off this site and back to the one being
retired.
